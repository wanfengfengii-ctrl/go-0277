# 粮食平房仓磷化氢熏蒸剂量与安全再入闭环

## 项目目标

建设一个可从零生成、可编译运行的 Go Web 系统，为粮食储备库执行单次平房仓磷化氢熏蒸闭环。系统以任务聚合为一致性边界，锁定仓房结构、粮种堆高、粮堆分区、设备与规则快照，随后驱动气密确认、分区施药、环流、浓度采集、定点剂量积分、确定性补药、泄漏传播冻结、散气和双人再入复核。核心证据包括“粮堆分区—逻辑时段”浓度覆盖格、分区及邻仓传播风险图、施药剂量守恒账簿、设备互斥租约和不可覆盖的测量代次。后端采用嵌入式持久化数据库、事务性事件记录和启动恢复扫描，前端提供任务总览、覆盖格、剂量账簿、风险图及操作表单。生产规模限定为20至30个 Go 文件、4至6个有业务含义的包、约2500行且保持在2000至3000有效生产 Go 行；规模由持久化、聚合状态机、租约、定点积分、传播冻结、设备适配和确定性错误处理自然形成。前端单独提供 package.json、提交的锁文件和固定版本构建命令，其代码不计入 Go 行数。

## 端到端业务流程

1. 目录配置与任务锁定：登记虚构仓房的额定仓容、允许粮种、分区邻接图、邻仓关系、施药笼、环流回路、采样管路和测点；创建任务后校验仓容、粮种及堆高，使用规则摘要执行条件更新，把结构、设备、药剂批次、采样时点、阈值、目标浓度—持续时间剂量和任务代次固化为不可变快照，状态由待锁定进入气密核验中。
2. 气密确认与原子施药：全部必需气密检查通过后，系统在单个事务内取得施药笼、风机回路和采样管路租约，登记分区投药计划并从药剂批次扣量；操作号重试返回既有结果，相同操作号对应不同内容则拒绝且不产生扣量、租约或状态残留。成功后进入施药中，并按锁定顺序记录各分区投放。
3. 环流与浓度采集：施药完成后按互斥风路切换环流并进入暴露维持中；脚本化设备适配器可能成功、拒绝、断连或超时，失败只创建带逻辑时间和确定重试次数的待重试调用。采样按测点、任务代次、补药代次和逻辑时段写入，先形成不可变原始记录，再校验是否可推进覆盖格。
4. 覆盖与有效暴露积分：每个分区必须在锁定采样窗内具备完整测点覆盖；引擎以整数定点浓度和逻辑时长计算浓度乘时梯形积分，统一执行符号、舍入、除零和溢出检查。重复、非锁定测点、越窗、错误代次以及已经关闭代次的迟到值仅保留为拒绝证据，不得覆盖当前格或改变积分。
5. 补药与泄漏封控：暴露窗结束后，剂量不足的分区按分区编号和时段排序计算唯一补药范围，在事务中创建一次新代次、扣减药量并进入补药中；并发请求只能有一个成功。任何测点或邻仓证据超过泄漏阈值时，沿锁定邻接图确定传播闭包，原子冻结闭包内相关风路、撤销其运行租约并进入泄漏封控中；未闭合风险不得散气。
6. 散气、再入与终局：所有分区达到目标剂量、账簿守恒且泄漏闭合后才可进入散气中；散气阶段要求每个安全测点在规定连续逻辑时段内低于再入阈值。两名身份不同且资格有效的人员分别复核后进入可再入，再由结案操作进入已完成。完成、风险隔离和已取消通过同一终局比较交换并发竞争，首个有效终局胜出，之后任何操作均只返回稳定终态错误且不改变证据。

## 核心组件与职责

1. 仓房与熏蒸规则目录：管理仓房容量、粮种约束、堆高换算、分区与邻仓图、设备拓扑、测点、药剂批次和版本化规则；生成规范排序后的摘要，供任务锁定及陈旧规则检测。
2. 熏蒸任务聚合：实现待锁定、气密核验中、施药中、暴露维持中、补药中、泄漏封控中、散气中、可再入、已完成、风险隔离、已取消状态机；维护快照、操作号幂等记录、版本号、审计事件和唯一终局。
3. 施药及风机占用管理器：以数据库唯一约束和事务管理施药笼、风机回路、采样管路租约，处理原子扣药、施药回执、互斥风路切换、传播冻结、设备调用及确定性重试队列。
4. 浓度覆盖与剂量积分引擎：维护分区—逻辑时段覆盖格和不可覆盖的测量代次，验证采样身份及窗口，使用整数定点算法完成仓容换算、药量核对、梯形积分、覆盖缺口报告和补药范围计算。
5. 泄漏散气及终局仲裁器：依据分区和邻仓邻接图计算泄漏传播闭包，检查风险闭合、散气连续安全覆盖、双人资格复核、剂量守恒，并原子裁定完成、风险隔离或取消。
6. Go HTTP API 与嵌入式前端：提供目录、任务命令、采样接入、设备重试、覆盖格、账簿、风险图和终局接口；提供可实际操作的浏览器页面，采用固定依赖版本构建后嵌入 Go 服务。

## 领域规则与不变量

1. 锁定是不可逆业务边界：必须固定仓房结构摘要、粮种、堆高、仓容换算参数、分区图、设备拓扑、药剂批次、采样时点、浓度阈值、目标剂量和任务代次；目录摘要变化不会静默影响已锁定任务，提交陈旧摘要必须返回 RULE_SNAPSHOT_STALE。
2. 施药质量以整数毫克记账，账簿满足批次初始量=可用量+预留量+已投量+已退量调整后的确定等式；每笔业务操作包含唯一操作号和规范化请求摘要。重复同内容幂等返回，重复不同内容返回 OPERATION_CONTENT_CONFLICT，失败事务不得遗留扣量或占用。
3. 同一时刻，同一施药笼、风机回路或采样管路只能归属于一个未释放任务租约；同一任务内互斥风路也不可并行运行。租约使用逻辑时钟、固定期限和版本条件更新，重启后根据持久化租约与未完成设备调用恢复，不依赖墙钟竞态。
4. 浓度采用整数微克每立方米，时长采用整数秒，体积采用整数立方分米，所有乘除使用检查型整数运算和规定的半向上舍入；负数、除零或中间乘积溢出分别返回稳定错误码，任何积分失败均不得部分更新覆盖格或累计剂量。
5. 原始测量键由任务、任务代次、补药代次、测点、逻辑时段和测量序号组成且不可修改。只有锁定测点、当前开放代次、合法窗口内的首个有效值可推进覆盖；相同键同值为幂等，相同键异值为冲突，旧代次迟到值只能追加拒绝记录。
6. 低浓度补药只允许从一次完整覆盖和积分结果派生；不足分区按编号归并为确定范围，每轮结果只能创建一个后继代次。泄漏超限优先于补药，必须计算传播闭包并原子冻结相关风路，闭包中任一风险未关闭时不得散气。
7. 设备拒绝、断连和超时只生成持久化待重试调用，重试序号、下次逻辑时间和最大次数由规则确定；系统不得补造浓度、施药成功或风机运行回执。超过重试上限后任务进入可仲裁的风险隔离条件，而非自动伪造完成。
8. 散气前必须同时满足全分区剂量达标、覆盖完整、药量守恒和泄漏闭合；再入要求所有锁定安全测点连续覆盖规定时段且均低于阈值，并由两名不同、资格在逻辑裁定时点有效的人员复核。终局采用唯一数据库记录和聚合版本竞争，终态绝不接受状态变更。

## 数据模型与持久化

1. Warehouse：仓房编号、额定仓容、允许粮种、结构版本、分区图、邻仓边、设备拓扑和规范摘要；Zone、NeighborEdge、Device、SamplingPoint 作为目录实体并有稳定排序键。
2. FumigationRule：粮种与堆高范围、仓容换算比例、气密要求、采样窗、目标浓度—持续时间剂量、泄漏阈值、再入阈值、连续安全时段、重试策略、定点精度及舍入规则。
3. FumigationTask：任务编号、聚合版本、状态、锁定快照、当前任务及补药代次、逻辑时钟、创建与终局信息；TaskEvent 保存递增序号、命令摘要、前后状态和确定性载荷，用于审计与启动恢复校验。
4. ResourceLease 与 DeviceCall：资源种类、资源编号、任务、租约版本、取得及到期逻辑时间、冻结原因；设备调用记录请求摘要、尝试次数、确定下次时间、结果或失败类别，支持重启续作。
5. DoseLedgerEntry 与 ApplicationRecord：药剂批次、分区、代次、预留、投放、退回、调整质量和操作号；批次余额及任务守恒值在同一事务中更新并可重算核验。
6. Measurement、CoverageCell 与 ExposureIntegral：保存不可变采样键、浓度、接收逻辑时间、接纳或拒绝原因；覆盖格按分区、逻辑时段、测点排序，积分记录输入版本、定点中间值、舍入余数和累计剂量。
7. LeakEvidence、RiskClosure 与 VentilationEvidence：泄漏源、阈值比较、传播闭包、冻结风路、风险关闭证据；散气测量保存安全窗覆盖与连续性结果。
8. OperationReceipt、ReentryReview 与 TerminalDecision：操作号和请求摘要对应的幂等结果；复核人、资格快照及逻辑时间；唯一终局种类、胜出版本、原因和证据摘要。

## 公开接口

1. 浏览器页面：任务列表与创建/锁定向导、状态时间线、分区浓度覆盖格、剂量账簿、设备租约、泄漏传播图、散气安全窗和双人复核面板；所有写操作显示操作号、稳定错误码及已排序原因，页面通过真实 HTTP API 读写。
2. 目录 API：管理和查询仓房、分区邻接图、设备拓扑、测点、药剂批次与规则版本；提供规范摘要预览及锁定前校验，不允许直接修改已锁定任务快照。
3. 任务命令 API：创建、锁定、提交气密结果、开始施药、登记施药回执、切换环流、创建补药、开始散气、提交再入复核以及完成、风险隔离、取消；写请求要求操作号、预期聚合版本和规范请求体。
4. 采样与设备 API：批量提交浓度或安全测量，返回逐项稳定结果；查询并执行到期设备调用重试。测试可注入脚本化传感器和风机适配器，生产入口使用同一接口而不绕过状态机。
5. 证据查询 API：按仓房、分区、时段、测点固定排序返回覆盖缺口、积分明细、剂量守恒、租约、泄漏闭包、散气连续覆盖、审计事件与终局证据；分页使用稳定复合游标。
6. 运行与构建接口：Go 服务启动时执行数据库迁移和恢复扫描，提供健康检查；前端目录包含 package.json、锁文件、固定 Node 版本说明及确定性 build 脚本，生成静态资产由 Go embed 提供。

## 失败边界

1. 所有改变任务、剂量、租约、覆盖、风险或终局的命令均在单个数据库事务内完成，使用聚合版本和唯一约束解决竞争；事务失败后不得留下部分状态，设备外部调用通过持久化意图记录在提交后执行。
2. 稳定错误响应包含 code、operation_id、aggregate_version 和 reasons；reasons 使用仓房编号、分区编号、逻辑时段、测点编号、原因码的升序比较器，禁止依赖 map 遍历、数据库自然顺序或墙钟生成结果。
3. 明确覆盖 WAREHOUSE_CAPACITY_MISMATCH、GRAIN_TYPE_MISMATCH、RULE_SNAPSHOT_STALE、RESOURCE_LEASE_CONFLICT、FAN_CIRCUIT_CONFLICT、DOSE_MASS_IMBALANCE、MEASUREMENT_MISSING、MEASUREMENT_CONFLICT、MEASUREMENT_OUT_OF_WINDOW、MEASUREMENT_GENERATION_STALE、FIXED_POINT_OVERFLOW、DEVICE_RETRY_EXHAUSTED、LEAK_PROPAGATION_ACTIVE、SUPPLEMENT_GENERATION_CONFLICT、OPERATION_CONTENT_CONFLICT 和 TERMINAL_ALREADY_DECIDED。
4. 进程可在事务提交前后或设备调用期间终止；重启时从数据库恢复非终态任务、有效租约、冻结图、覆盖格、账簿和待重试调用，通过操作回执避免重复扣药或重复推进状态。恢复校验若发现持久化证据不守恒，任务只能进入风险隔离仲裁路径。
5. 乱序与迟到采样可以持久化为原始证据，但只有当前开放代次内满足窗口和唯一键规则的数据可改变覆盖；批量请求中任一业务项冲突时整批不推进覆盖，返回全部确定排序原因。
6. 并发终局共享一个唯一决策点；完成、风险隔离与取消中只有首个满足前置条件且成功提交者生效，失败竞争者不得释放额外资源、改写事件、复核或终局时间。

## 验收标准

1. 锁定操作完整固化仓房结构摘要、粮种与堆高、分区图、施药笼、药剂批次、环流回路、采样时点、阈值、目标剂量和任务代次；仓容或粮种错配及陈旧摘要以确定原因拒绝且任务仍为待锁定。
2. 冲突任务不能同时占用同一风机回路、施药笼或采样管路；并发施药、换风路与扣药原子裁定，操作号同内容重试不重复扣量，同号异内容拒绝，重启后租约和账簿保持一致。
3. 每个分区形成完整浓度时间序列；缺失、重复异值、越窗、错误代次、非锁定测点和旧代次迟到数据均不能推进覆盖或积分，错误原因严格按仓房、分区、时段、测点排序。
4. 所有浓度、时间、药量、仓容换算和浓度乘时积分使用规定整数定点算法；符号、半向上舍入、除零和乘加溢出有精确断言，失败不产生部分覆盖或累计剂量。
5. 脚本化传感器和风机的拒绝、断连、超时仅产生带逻辑时间及确定重试次数的持久化调用；重启可继续重试，且任何失败路径都不会伪造测量、施药或运行回执。
6. 剂量不足只按确定分区范围创建一个新补药代次；泄漏超限沿邻接图计算闭包并原子冻结相关风路，旧代次迟到值不覆盖证据，风险关闭前不得散气。
7. 只有全分区剂量达标、覆盖完整、药量守恒且泄漏闭合才能散气；连续安全测点覆盖和两名不同合格人员复核后才可再入，完成前置条件可从持久证据完全重算。
8. 完成、风险隔离和取消并发竞争只产生一个终局，终态后任何命令均不改变任务、剂量、租约、覆盖或事件；项目满足20至30个生产 Go 文件、2000至3000有效生产 Go 行、至少4个业务包，并具有可确定构建的真实前端。

## 确定性测试场景

1. 锁定成功及快照不可变；分别断言仓容错配、粮种错配、规则摘要陈旧，并验证多项原因的固定排序。
2. 两个任务经同步屏障同时争夺同一施药笼、风机回路和采样管路，精确断言仅一方成功，失败方无租约、扣量或事件残留。
3. 施药操作号同内容重复提交只扣减一次；同一操作号更换分区或质量返回内容冲突；在事务故障注入后验证账簿守恒。
4. 提交固定分区与时段的完整整数浓度序列，精确断言覆盖格和梯形积分；覆盖缺点、重复异值、非锁定测点、越窗、乱序和旧代次迟到值均不能推进结果。
5. 构造半向上舍入边界、零时长、负浓度、仓容除零、最大整数乘法及累计加法溢出，精确断言错误码和事务回滚。
6. 用脚本化设备依次返回超时、断连、拒绝和成功，以逻辑时钟推进重试，断言尝试序号和下次时间；在中途重启存储库后继续执行且不产生伪造回执。
7. 多个并发请求针对同一低浓度结果创建补药，断言只有一个新代次和一次扣药；向旧代次补交高浓度值，确认当前积分及补药决定不变。
8. 使用虚构仓房邻接图制造单点泄漏，断言传播闭包、风路冻结顺序和补药阻断；关闭部分风险仍禁止散气，全部闭合后才恢复资格。
9. 全分区达标但药量不守恒、覆盖不全或存在活动泄漏时分别拒绝散气；连续安全窗中插入缺测或超阈值样本应重置连续性。
10. 同一人员重复复核、资格过期和两名不同合格人员复核的结果可确定；仅最后一种允许进入可再入。
11. 使用同步屏障同时提交完成、风险隔离和取消，重复运行精确断言数据库仅有一个终局且所有失败竞争者返回 TERMINAL_ALREADY_DECIDED。
12. 关闭并重新打开数据库，验证非终态任务、租约、覆盖格、账簿、风险闭包、设备重试和幂等回执完整恢复；公开测试分布在至少4个测试文件，总数不少于12个独立用例。

## 组件追踪关系

1. 仓房与熏蒸规则目录对应锁定验收，覆盖仓容/粮种错配、结构摘要和规则陈旧测试，并为所有确定排序提供稳定业务键。
2. 熏蒸任务聚合对应完整状态机、操作号幂等、重启恢复和并发终局测试，是状态及事务一致性的主边界。
3. 施药及风机占用管理器对应资源竞争、剂量扣减、风路切换、设备故障重试和传播冻结测试。
4. 浓度覆盖与剂量积分引擎对应完整序列、缺测与重复、乱序迟到、代次隔离、定点舍入及溢出边界测试。
5. 泄漏散气及终局仲裁器对应邻接传播、补药唯一代次、风险闭合、连续安全覆盖、双人复核和唯一终局测试。
6. Go HTTP API 与嵌入式前端对应真实页面/API联通、稳定错误结构、固定排序查询、数据库启动恢复及确定性前端构建验收。

## 独特性

该计划聚焦粮食储备库平房仓磷化氢熏蒸，把浓度覆盖格、不可覆盖的测量代次、药剂质量守恒、风机互斥租约和邻仓泄漏传播闭包组合为同一安全再入闭环；标题及 slug 均不复用既有项目的品牌式命名或联合检验放行模式。
